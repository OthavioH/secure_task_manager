{
  "name": "backend",
  "version": "1.0.0",
  "type": "commonjs",
  "scripts": {
    "start": "node dist/src/server.js",
    "start:prod": "npm run build && npm run start",
    "dev": "nodemon --watch src --ext ts,json --exec ts-node src/server.ts",
    "dev:db": "docker-compose -f ./docker-compose.db.yml up -d",
    "build": "tsc",
    "typeorm": "npx typeorm-ts-node-commonjs --dataSource dist/config/db_data_source.js",
    "typeorm:migrate": "npm run typeorm migration:run",
    "typeorm:generate": "npm run typeorm migration:generate migrations/migration --timestamp",
    "typeorm:revert": "npm run typeorm migration:revert"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "@fastify/cors": "^11.1.0",
    "crypto": "^1.0.1",
    "dotenv": "^17.2.2",
    "fastify": "^5.5.0",
    "mysql2": "^3.14.4",
    "pino-pretty": "^13.1.1",
    "typeorm": "^0.3.26"
  },
  "devDependencies": {
    "@types/node": "^24.3.1",
    "nodemon": "^3.1.10",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.9.2"
  }
}
